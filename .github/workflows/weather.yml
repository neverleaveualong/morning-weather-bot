name: weather

on:
  schedule:
    # 08:00 KST = 23:00 UTC on the previous day.
    - cron: "0 23 * * *"
    # 20:00 KST = 11:00 UTC.
    - cron: "0 11 * * *"
  workflow_dispatch:

jobs:
  send-weather:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Send weather notification
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          WEATHER_BOT_TITLE: ${{ secrets.WEATHER_BOT_TITLE }}
          EVENING_REPORT_TITLE: ${{ secrets.EVENING_REPORT_TITLE }}
          GOOGLE_CALENDAR_ICAL_URL: ${{ secrets.GOOGLE_CALENDAR_ICAL_URL }}
        run: python weather_bot.py
